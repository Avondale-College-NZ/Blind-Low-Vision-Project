@page
@model AuthSystem.Views.Members.IndexModel

@{
    ViewData["Title"] = "MembersList";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MembersModel[0].MembersId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MembersModel[0].MembersName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MembersModel[0].EmailAddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MembersModel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MembersId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MembersName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailAddress)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
